generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Master {
  id         String @id @default(cuid())
  fullName   String
  university String
  email      String @unique
  password   String
}
